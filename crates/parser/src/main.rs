#![feature(portable_simd)]

mod scopes;
mod throughput;
mod regions;

fn main() {
  // let content = include_bytes!("input.html");
  // println!("Hello, world!");

  // let result = throughput::Throughput::compute(270_000, content.len(), || {
  //   html::parse_html(content);
  // });

  // let result = throughput::Throughput::compute(270_000, content.len(), || {
  //   html_fast::parse_html(content);
  // });

  // println!("{}", result);
}
